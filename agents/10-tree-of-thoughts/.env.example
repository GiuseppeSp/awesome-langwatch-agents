# Required
OPENAI_API_KEY=sk-...
LANGWATCH_API_KEY=sk-...

# Optional — defaults shown
TOT_MODE=cot        # cot | tot
MODEL=gpt-4o-mini
TEMPERATURE=0
TOT_BEAM=3          # states kept per search depth
TOT_PROPOSALS=4     # moves the generator suggests per state
