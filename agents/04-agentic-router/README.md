# 04 · Agentic Router

An LLM that decides which downstream pipeline to call (knowledge-base RAG vs general LLM vs web search). Built on the same pattern as [Nonna's Cookbook](https://github.com/GiuseppeSp/my_App) — generalized so you can drop in any domain.

## What you'll learn

How to build a reliable LLM router (three prompt tricks that actually move the needle), and how to use exact-match evaluators to catch routing regressions before they hit production.

## The tuning experiment (placeholder)

> **Three prompt tricks that made the router 30% more reliable**
>
> _TBD._

## Status

⏳ Planned. (Architecture already battle-tested in Nonna's Cookbook — this version extracts the generic core.)
